package hr.fer.zemris.java.custom.scripting.lexer;

/**
 * Enum for states of Lexer.
 * */
public enum SmartLexerState {
  /**
   * TEXT<br>
   * Used for TEXT state
   * */
  TEXT,
  /**
   * TAG <br>
   * Used for TAG state<br>
   * */
  TAG;
}
